# 学习这一块
## 第一天  
    1. 首先是交流电用负数表示 用到np的j 注意这个j在乘其他数的时候 要乘号就是 , np.abs()算幅值 np.angle(   , deg = True) 算角度
    2. 复杂电路的矩阵求解 我们用到了np.linalg,solve(矩阵，向量)  
    3. 物理可视化 np.linspace() 采样点 然后用plt画图 画图要注意负数画图 要幅值 或者角度 选择画
    4. pandas的时间序列,时间这一块，就是说要用到 pd.to_datatime,resample 重采样 也和分组聚合很像 就是分组 然后你说一个目标 之后聚合 
    5. 物理除噪 先算Wn  Wn就是 截止频率 / 采样频率的一半 然后用 singal.butter来就可以了 这个主要是除噪要用
    6. fft频域特征提取 有时候数据不是一个sin 是很多sin 就可以用傅里叶变换， 提取出不同w 就ok